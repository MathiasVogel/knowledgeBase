---
slug: ssh-windows
title: SSH auf Windows
---

### Speicherort der SSH-Keys
Unter Windows werden die SSH-Keys im Verzeichnis `C:\Users\<username>\.ssh` gespeichert.
Falls noch nie mit SSH gearbeitet wurde, existiert dieses Verzeichnis noch nicht. Es muss dann manuell erstellt werden.